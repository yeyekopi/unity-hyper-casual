using NUnit;
using NUnit.Framework;
using static GridExtensions;

public class BubbleTest {
    // [Test]
    // public void GridAndPosition() {
    //     for (var i = 0; i < CELL_COUNT; i++) {
    //         var v2 = GetPositionFromGridIndex(null, i);
    //         var gridIdx = GetGridIndexFromPosition(null, v2);
    //         Assert.That(gridIdx, Is.EqualTo(i));
    //     }
    // }
}
